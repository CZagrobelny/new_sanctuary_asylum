<% unless current_user.admin? %>
  <div class='row'><%= link_to 'Back to Friend Record', friend_path(@friend, tab: '#asylum') %></div>
<% end %>
<h2>New Asylum Application Draft</h2>
<%= render 'form' %>