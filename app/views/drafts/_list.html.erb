<% if friend.releases.any? || current_community.primary? %>
  <%= render 'drafts/friend_released', friend: friend %>
<% else %>
  <%= render 'drafts/friend_needs_release', friend: friend %>
<% end %>
