<h1><%=  @friend.name %></h1>

<div class= 'row'>
  <div class='col-md-10 col-md-offset-1'>
    
    <!-- Nav tabs -->
    <ul class="nav nav-tabs" role="tablist">
      <li class="nav-item">
        <a class="nav-link" data-toggle="tab" href="#info" role="tab">Basic</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active" data-toggle="tab" href="#documents" role="tab">Documents</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" data-toggle="tab" href="#sijs" role="tab">SIJS</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" data-toggle="tab" href="#access" role="tab">Access</a>
      </li>
    </ul>

    <!-- Tab panes -->
    <div class="tab-content form-inputs">
      <div class="tab-pane active" id="info" role="tabpanel">
        <%= render 'info', friend: @friend %>
      </div>
      <div class="tab-pane" id="documents" role="tabpanel">
        <%= render 'application_drafts', friend: @friend %>
      </div>
      <div class="tab-pane" id="sijs" role="tabpanel">
        <%= render 'sijs', friend: @friend %>
      </div>
      <div class="tab-pane" id="access" role="tabpanel">
        <%= render 'access', friend: @friend %>
      </div>
    </div>
  </div>
</div>

<script>
  $(document).ready(function () {
    $('a[href="<%= @current_tab %>"]').click();
  });
</script>
